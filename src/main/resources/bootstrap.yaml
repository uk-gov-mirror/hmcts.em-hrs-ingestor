spring:
  cloud:
    propertiesvolume:
      enabled: true
      prefixed: false
      paths: /mnt/secrets/em-hrs
      aliases:
        microservicekey-em-hrs-ingestion: S2S_KEY
        AppInsightsInstrumentationKey: APPINSIGHTS_INSTRUMENTATIONKEY
        hrs-POSTGRES-PASS: SPRING_DATASOURCE_PASSWORD
        notification-api-key: NOTIFICATION_API_KEY
