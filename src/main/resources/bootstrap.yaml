spring:
  cloud:
    propertiesvolume:
      enabled: true
      prefixed: false
      paths: /mnt/secrets/em-hrs-ingestor
      aliases:
        microservicekey-em-hrs-ingestor: S2S_KEY
        AppInsightsInstrumentationKey: APPINSIGHTS_INSTRUMENTATIONKEY
        cvp-storage-connection-string: CVP_STORAGE_CONNECTION_STRING
        cvp-storage-container-name: CVP_STORAGE_CONTAINER_NAME
